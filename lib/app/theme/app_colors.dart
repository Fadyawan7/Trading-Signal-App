import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0A0F14);
  static const card = Color(0xFF131A22);
  static const primary = Color(0xFF22C55E);
  static const accent = Color(0xFF14552C);
  static const secondary = Color(0xFF1A2832);
  static const mutedText = Color(0xFF9CA3AF);
  static const text = Colors.white;
  static const border = Color(0x1A22C55E);
}
